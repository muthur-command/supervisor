# Default to PulseAudio

pcm.!default {
    type pulse
    hint {
        show on
        description "Default ALSA Output (Muthur Command PulseAudio Sound Server)"
    }
}

ctl.!default {
    type pulse
}
